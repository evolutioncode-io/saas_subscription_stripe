

<div style="width: 250px;">
  <%= stripe_connect_button %>
</div>

<h1>Pricing</h1>
<br>
<hr>
<h2>Basic</h2>
    <% @pricing_basic.each do |price| %>
      <b>
        <%= price.lookup_key %>
      </b>
      <small><%= price.id %></small>
      <b>
        <%= price.unit_amount/100 %>
        <%= price.currency %>
        <%= price.recurring.interval %>
      </b>
      <%= link_to "Pay #{price.unit_amount/100} per #{price.recurring.interval}", checkout_create_path(price: price.id), method: :post, remote: true, data: { disable_with: "validating..."} %>
      <br>
    <% end %>
<hr>

<h2>Team</h2>
    <% @pricing_team.each do |price| %>
      <b>
        <%= price.lookup_key %>
      </b>
      <small><%= price.id %></small>
      <b>
        <%= price.unit_amount/100 %>
        <%= price.currency %>
        <%= price.recurring.interval %>
      </b>
      <%= link_to "Pay #{price.unit_amount/100} per #{price.recurring.interval}", checkout_create_path(price: price.id), method: :post, remote: true, data: { disable_with: "validating..."} %>
      <br>
    <% end %>

<hr>
<h2>Enterprise</h2>
    <% @pricing_enter.each do |price| %>
      <b>
        <%= price.lookup_key %>
      </b>
      <small><%= price.id %></small>
      <b>
        <%= price.unit_amount/100 %>
        <%= price.currency %>
        <%= price.recurring.interval %>
      </b>
      <%= link_to "Pay #{price.unit_amount/100} per #{price.recurring.interval}", checkout_create_path(price: price.id), method: :post, remote: true, data: { disable_with: "validating..."} %>
      <br>
    <% end %>
